=
" SELECT GCD.REQUESTNUMBER                 REQUISICAO,GCD.STATUS,  "  &
"       GCD.LOCATION                      LOCAL, "  &
"       GCD.NOOFCOUNTS                    CONT_SOLICITADA, "  &
"       GCD.NOOFCOUNTED                   CONT_REALIZADA,  "  &
"       'PENDENTE CONTAGEM SUPERVISOR'    STATUS, "  &
"       CASE WHEN TO_CHAR(GRI.SIMULATION) = '0'  "  &
"              THEN 'NÃO' "  &       
"            ELSE   'SIM' "  &       
"       END                               SIMULACAO "  &       
" "  &                              
" FROM  " + Parameters!Compania.Value + ".GLOBALINVCOUNTDETAIL GCD  "  &
"                      "  &
" INNER JOIN " + Parameters!Compania.Value + ".GLOBALINVREQUEST GRI "  &
"        ON GRI.REQUESTNUMBER = GCD.REQUESTNUMBER "  &
"                      "  &
" WHERE GCD.NOOFCOUNTS = GCD.NOOFCOUNTED  "  &
"       AND GCD.STATUS = '07' "  &
"                      "  &
"  AND CASE WHEN TO_CHAR(GRI.SIMULATION) = '0'   "  &
"              THEN 'NÃO' "  &
"            ELSE   'SIM' "  &
"       END IN (:Simulacao) "
